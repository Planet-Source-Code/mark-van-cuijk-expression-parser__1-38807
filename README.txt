The ExpressionParser.bas file is the modules containing the code needed for parsing expressions. The others files are just for an example application. Just run the ExprParse.vpb project to see the code working.

- Mark